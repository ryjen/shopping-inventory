# Deprecated: ADR-0005 moved

This file path is deprecated because it used the older provider-specific Supabase migration framing.

Use the provider-agnostic ADR instead:

- [ADR-0005: Defer Backend Provider Selection](ADR-0005-defer-backend-provider-selection.md)

The decision is to keep the MVP spreadsheet-first, defer backend provider selection, and evaluate SQLite, PocketBase, hosted Postgres-compatible systems, and other candidates against project requirements before choosing a provider.
