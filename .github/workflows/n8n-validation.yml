name: Repository validation

on:
  pull_request:
    paths:
      - '.github/pull_request_template.md'
      - '.github/workflows/n8n-validation.yml'
      - 'README.md'
      - 'automation/n8n/**'
      - 'docs/**'
      - 'evaluation/**'
      - 'migrations/**'
      - 'package.json'
      - 'schemas/**'
      - 'scripts/test-n8n-import.sh'
      - 'src/**'
      - 'test/**/*.mjs'
      - 'test/fixtures/n8n/**'
      - 'wrangler.toml'
  push:
    branches:
      - main
    paths:
      - '.github/pull_request_template.md'
      - '.github/workflows/n8n-validation.yml'
      - 'README.md'
      - 'automation/n8n/**'
      - 'docs/**'
      - 'evaluation/**'
      - 'migrations/**'
      - 'package.json'
      - 'schemas/**'
      - 'scripts/test-n8n-import.sh'
      - 'src/**'
      - 'test/**/*.mjs'
      - 'test/fixtures/n8n/**'
      - 'wrangler.toml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repository-tests:
    name: contract, n8n, and Cloudflare tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run canonical contract tests
        run: node --test test/canonical-contracts.test.mjs

      - name: Run n8n unit tests
        run: npm run test:n8n:unit

      - name: Run n8n integration tests
        run: npm run test:n8n:integration

      - name: Run n8n e2e simulation tests
        run: npm run test:n8n:e2e

      - name: Run Cloudflare storage tests
        run: npm run test:cloudflare

  n8n-cli-import-smoke-test:
    name: n8n CLI import smoke test
    runs-on: ubuntu-latest
    needs: repository-tests

    env:
      DB_TYPE: sqlite
      DB_SQLITE_POOL_SIZE: '1'
      N8N_ENCRYPTION_KEY: shopping-inventory-ci-test-key

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install n8n CLI
        run: npm install -g n8n

      - name: Import workflow exports with n8n CLI
        run: npm run test:n8n:import
