name: Repository validation

on:
  pull_request:
    paths:
      - 'automation/n8n/**'
      - 'src/**'
      - 'migrations/**'
      - 'scripts/test-n8n-import.sh'
      - 'test/**/*.mjs'
      - 'test/fixtures/n8n/**'
      - 'package.json'
      - 'wrangler.toml'
      - '.github/workflows/n8n-validation.yml'
  push:
    branches:
      - main
    paths:
      - 'automation/n8n/**'
      - 'src/**'
      - 'migrations/**'
      - 'scripts/test-n8n-import.sh'
      - 'test/**/*.mjs'
      - 'test/fixtures/n8n/**'
      - 'package.json'
      - 'wrangler.toml'
      - '.github/workflows/n8n-validation.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  repository-tests:
    name: n8n and Cloudflare unit/integration tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run n8n unit tests
        run: npm run test:n8n:unit

      - name: Run n8n integration tests
        run: npm run test:n8n:integration

      - name: Run n8n e2e simulation tests
        run: npm run test:n8n:e2e

      - name: Run Cloudflare storage tests
        run: npm run test:cloudflare

  n8n-cli-import-smoke-test:
    name: n8n CLI import smoke test
    runs-on: ubuntu-latest
    needs: repository-tests

    env:
      DB_TYPE: sqlite
      DB_SQLITE_POOL_SIZE: '1'
      N8N_ENCRYPTION_KEY: shopping-inventory-ci-test-key

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install n8n CLI
        run: npm install -g n8n

      - name: Import workflow exports with n8n CLI
        run: npm run test:n8n:import
